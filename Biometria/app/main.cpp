#include "libs/functions.hpp"

int main(int argc, char const *argv[]) {

    // if (argc < 2) {
    //     std::cout << "Usage: ./main img1 img2" << std::endl;
    //     // exit(-1);
    // }
    //
    // Mat user1, user2;
    // vector<Point> contourUser1, contourUser2;
    // user1 = imread(argv[1], 0);
    // user2 = imread(argv[2], 0);

    // contourUser1 = getHandContour(user1);
    // contourUser2 = getHandContour(user2);
    // imshow("User 1", Mat(contourUser1));
    // imshow("User 2", contourUser2);
    // cv::waitKey(0);

    // saveContour(contourUser1, argv[2]);
    // login(contourUser1);

    // measureDistance();
    // buildDatabase();
    doStuff();

    return 0;
}
